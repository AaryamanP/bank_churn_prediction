# Minimal requirements for churn_prediction
# Only the packages you said you use are listed here.
# Assumption: CPU-only PyTorch install. If you need GPU/CUDA support,
# install PyTorch following the official instructions at https://pytorch.org

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.2.0
torch>=2.0.0